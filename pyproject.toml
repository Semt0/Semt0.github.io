[project]
name = "semt0-blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymupdf>=1.24.0",
]

[dependency-groups]
dev = [
    "zensical>=0.0.23",
]
